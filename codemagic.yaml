workflows:
  android-apk:
    name: Arise Up - Android APK
    max_build_duration: 60
    environment:
      node: 20.17.0
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Set up EAS
        script: npm install -g eas-cli expo-cli
      - name: Run Expo prebuild
        script: npx expo prebuild --platform android --clean
      - name: Build APK
        script: |
          cd android
          ./gradlew assembleRelease
    artifacts:
      - android/app/build/outputs/apk/release/*.apk
    publishing:
      email:
        recipients:
          - ravi2009u@gmail.com
        notify:
          success: true
          failure: true
